# Transcription of audio/video files in WSL2 (Ubuntu) + NVIDIA GPU

## Objective

The objective is to run local transcription of audio or video files using an NVIDIA graphics card on **Windows 11 + WSL2 + Ubuntu**.